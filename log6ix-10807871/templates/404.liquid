	
	<div class="header-component page">
		<div class="container">
			<img src="{{ 'about-hero.png' | asset_url }}" alt="{{ page.title }} | Log6ix">
		</div>
	</div>
	
	<div class="page-content {{ page.handle }}">
		<div class="container">
			<div class="row">
				<div class="col-sm-9 col-sm-offset-3">
					<h1 class="centered">404 Page Not Found</h1>
					<h2 class="delta centered">The page you requested does not exist. Click <a href="/">here</a> to continue shopping.</h2>
				</div>
			</div>
		</div>
	</div>